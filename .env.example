# ─── App ───────────────────────────────────────────────
NODE_ENV=development
PORT=3000

# Comma-separated allowed CORS origins (web preview, Tauri, RN dev server).
# Defaults to local dev origins when unset. Set real origins in production.
CORS_ORIGINS=http://localhost:1420,http://tauri.localhost,https://tauri.localhost

# ─── Postgres (matches docker-compose.yml) ─────────────
# Format: postgresql://USER:PASSWORD@HOST:PORT/DB?schema=public
DATABASE_URL=postgresql://pfos:pfos_local_pw@localhost:5432/patient_flow_os?schema=public

# ─── Redis (queue engine — required, not optional) ─────
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ─── Auth ──────────────────────────────────────────────
JWT_SECRET=replace_me_with_long_random_string
OTP_EXPIRY_SECONDS=300

# ─── Voice API (shared secret with the voice agent) ────
# Server-to-server secret guarding /voice/*. MUST equal the voice agent's
# VOICE_INTERNAL_SECRET — same name, same value. Unset -> /voice/* is 401.
VOICE_INTERNAL_SECRET=replace_me_with_long_random_string

# ─── MSG91 (patient/staff OTP via SMS) ─────────────────
MSG91_AUTH_KEY=
MSG91_SENDER_ID=
MSG91_OTP_TEMPLATE_ID=

# Default recipient for POST /integrations/test-sms (admin connectivity check).
# 10-digit Indian mobile or full number without '+'. Optional.
INTEGRATIONS_TEST_MOBILE=

# ─── Razorpay (payments — sandbox first) ───────────────
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=

# ─── Firebase Cloud Messaging (push) ───────────────────
# Path to service-account json, OR inline the JSON via FCM_SERVICE_ACCOUNT_JSON.
# Push stays in dev-log mode until one of these is set.
FCM_SERVICE_ACCOUNT_PATH=./secrets/fcm-service-account.json
FCM_SERVICE_ACCOUNT_JSON=

# ─── Notification thresholds (queue position crossings) ─
# Fire "approaching" when patients-ahead <= this; "arrival" likewise. Optional.
NOTIFY_APPROACHING_AHEAD=3
NOTIFY_ARRIVAL_AHEAD=1
